# Hosted Supabase assets

Phase 1 will initialize the Supabase CLI configuration, migrations, generated
database types, and remote verification scripts in this directory.

Reflow uses a linked hosted development project. Do not run `supabase start` or
introduce Docker-based local services.
